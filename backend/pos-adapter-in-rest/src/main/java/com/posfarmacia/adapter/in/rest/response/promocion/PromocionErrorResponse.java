package com.posfarmacia.adapter.in.rest.response.promocion;

/** Cuerpo de error para el sub-contexto de promociones (Word, seccion 10: codigo + mensaje). */
public record PromocionErrorResponse(String codigo, String mensaje) {
}
